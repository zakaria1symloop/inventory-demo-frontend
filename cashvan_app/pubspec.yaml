name: cashvan_app
description: "Tracksera Cashvan - البيع المتنقل"
publish_to: 'none'

version: 1.0.6+4

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.4.10

  # HTTP & API
  dio: ^5.9.0

  # Storage
  shared_preferences: ^2.5.3

  # Location
  geolocator: ^14.0.2

  # UI
  google_fonts: ^6.2.1
  flutter_spinkit: ^5.2.2

  # Utils
  intl: ^0.20.2
  connectivity_plus: ^7.0.0
  uuid: ^4.5.2
  url_launcher: ^6.2.5

  # Printing
  printing: ^5.13.4
  pdf: ^3.11.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
